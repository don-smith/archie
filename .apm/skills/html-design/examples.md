<!-- GENERATED FILE. DO NOT EDIT. Build from canonical design-system records. -->

# Selected examples

Copy only after choosing the profile, information need, or relationship. Adapt IDs and content. Keep canonical classes.

- **Statement hero**. Pattern `statement-hero`. File: `examples/patterns/statement-hero.html`.
- **Evidence matrix**. Pattern `evidence-matrix`. File: `examples/patterns/evidence-matrix.html`.
- **Form field**. Pattern `form-field`. File: `examples/patterns/form-field.html`.
- **Status banner**. Pattern `status-banner`. File: `examples/patterns/status-banner.html`.
- **Empty state**. Pattern `empty-state`. File: `examples/patterns/empty-state.html`.
- **System context**. Diagram recipe `system-context`. File: `examples/diagrams/system-context.html`.
- **Lifecycle**. Diagram recipe `lifecycle`. File: `examples/diagrams/lifecycle.html`.

The living catalog contains the complete registry. These bundled examples cover the common document, application, and diagram paths without duplicating every catalog entry.
