import type { NormalizedGraphV1 } from "../formats/types.js";
/** Produces a deterministic, evidence-only reading view of one normalized graph. */
export declare function buildOnboardingSummary(graph: NormalizedGraphV1): string;
